# Server Configuration
server.port=8081

# MongoDB Configuration
spring.data.mongodb.host=mongodb
spring.data.mongodb.port=27017
spring.data.mongodb.database=flouswise
spring.data.mongodb.username=admin
spring.data.mongodb.password=password123
spring.data.mongodb.authentication-database=admin

# JWT Configuration
# IMPORTANT: This secret MUST be the same as auth-service!
# The secret will be injected via environment variable from docker-compose
app.jwt.secret=${JWT_SECRET}
