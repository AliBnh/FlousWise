# ============================================================================
# FlousWise AI Service - Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values

# Service Configuration
ENVIRONMENT=development
SERVICE_NAME=ai-service
LOG_LEVEL=INFO

# JWT Authentication (REQUIRED - must match auth-service)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_ALGORITHM=HS256

# MongoDB Configuration
MONGODB_URI=mongodb://admin:password123@localhost:27017/flouswise?authSource=admin
MONGODB_DATABASE=flouswise

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Finance Service Integration
FINANCE_SERVICE_URL=http://localhost:8081
PROFILE_CACHE_TTL=300

# Ollama LLM Configuration
# For Docker: use http://host.docker.internal:11434
# For local: use http://localhost:11434
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=mistral
OLLAMA_TIMEOUT=60

# Embedding Model Configuration
EMBEDDING_MODEL=all-MiniLM-L6-v2

# ChromaDB Vector Database
CHROMA_PERSIST_DIR=./chroma_db
CHROMA_COLLECTION_NAME=finance_books

# RAG Configuration
RAG_TOP_K=5

# Moroccan Context Data
MOROCCAN_CONTEXT_FILE=./data/moroccan_context.json

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# ============================================================================
# SETUP INSTRUCTIONS:
# ============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Update JWT_SECRET to match your auth-service
#
# 3. Install Ollama and download Mistral:
#    ollama pull mistral
#
# 4. Add finance books to data/books/ directory
#
# 5. Run book ingestion:
#    python scripts/ingest_books.py
#
# 6. Start the service:
#    python -m app.main
#    # or with uvicorn:
#    uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
#
# ============================================================================
